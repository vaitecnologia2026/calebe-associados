// Persona SECRETARY (reservas) · vê o mesmo painel de Estrutura do admin
import { Estrutura } from "@/features/admin/routes/Estrutura";

export function ReservasEstrutura() {
  return <Estrutura />;
}
